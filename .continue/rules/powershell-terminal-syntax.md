---
alwaysApply: true
---

Terminal-Befehle IMMER in PowerShell-Syntax schreiben: `Set-Location` statt `cd`, `;` als Trennzeichen statt `&&`. Niemals Unix-Shell-Syntax in run_terminal_command verwenden.
# Sprint 1.1 – Gradle & Bootstrap

> Roadmap: Phase 1, Sprint 1.1
> Vorlage: VillagerAI build.gradle.kts (ohne Gson)
> Ziel: Build mit `.\gradlew.bat shadowJar` erfolgreich

## Vorarbeiten
- [x] `.continue/rules/` auf Seasons-spezifisch prüfen (Provider/Quest entfernt?)
- [x] `docs/architecture-concept.md` gelesen
- [x] VillagerAI `build.gradle.kts` als Referenz gelesen

## Dateien
| # | Datei | Inhalt | Status |
|---|-------|--------|--------|
| 1 | `build.gradle.kts` | Paper 1.21.5, Java 21, shadowJar, group=de.ajsch, version=0.1.0-SNAPSHOT, keine Gson | [x] |
| 2 | `settings.gradle.kts` | `rootProject.name = "Seasons"` | [x] |
| 3 | `src/main/resources/plugin.yml` | name: Seasons, version: ${version}, api-version: "1.21", commands + permissions | [x] |
| 4 | `src/main/java/de/ajsch/seasons/SeasonsPlugin.java` | onEnable/onDisable, Logger | [x] |
| 5 | `.gitignore` prüfen | build/, .gradle/, *.jar (außer wrapper), IDE-Dateien | [x] |

## Build-Test
- [x] `.\gradlew.bat compileJava` – keine Fehler
- [x] `.\gradlew.bat shadowJar` – erzeugt `build/libs/Seasons-0.1.0-SNAPSHOT.jar`
- [x] `.\gradlew.bat build` – erzeugt zusätzlich `-plain.jar`

## Abnahmekriterien
- [x] `shadowJar` läuft sauber durch ohne Compiler-Fehler
- [x] `Seasons-0.1.0-SNAPSHOT.jar` existiert (nicht nur `-plain.jar`)
- [x] `plugin.yml` enthält `/season` Command mit `seasons.info`, `seasons.debug`, `seasons.admin` Permissions
- [x] `.gitignore` schließt `build/`, `.gradle/`, `*.jar` (außer `gradle-wrapper.jar`) aus
- [x] Projekt kompiliert mit Java 21
